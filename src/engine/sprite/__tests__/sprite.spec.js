const expect = require( 'chai' ).expect;
const { SpriteImage } = require( '../sprite.js' );

describe( 'Sprite class/object', () => { 

    it( 'should slice an image into a number of slices', () => { 


        

    } )

} );