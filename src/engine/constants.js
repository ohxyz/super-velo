const NONE = 0
const UP = 1;
const RIGHT = 2;
const DOWN = 3;
const LEFT = 4;

const REFRESH_RATE = 60;

export {

    NONE,
    UP, 
    RIGHT, 
    DOWN, 
    LEFT, 
    REFRESH_RATE,
};